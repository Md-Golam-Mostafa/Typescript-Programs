let firstName: string;  let lastName: string;
let fullName: string;   let userId:number;
let isActivated:boolean;

firstName = "Md.Golam"; lastName = "Mostafa";
fullName = firstName.concat(lastName);

userId = 102;   isActivated= true;

console.log(`User ${userId} is activated on ${isActivated} his first name: ${firstName} and ${lastName} so full  name: ${fullName}`); 
